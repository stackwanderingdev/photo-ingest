# Functional Requirement Files

Copy `REQ-template.md` once for each atomic behavior. Concrete filenames begin
with their identifier and a lowercase hyphenated title. Keep the parent index in
`../functional-requirements.md` synchronized.
