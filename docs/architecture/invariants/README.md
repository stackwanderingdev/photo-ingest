# System Invariant Files

Copy `INV-template.md` once for each independently referenceable invariant.
Concrete filenames begin with their identifier and a lowercase hyphenated title.
Keep the parent index in `../system-contracts.md` synchronized.
